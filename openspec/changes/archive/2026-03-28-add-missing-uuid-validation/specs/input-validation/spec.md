<!-- No spec changes required. The existing input-validation spec already defines
     the "Device uuid validation" requirement mandating assertValidUuid(uuid, 'uuid')
     in every controller that accepts a device uuid. This change brings the
     implementation into compliance with that existing requirement. -->
