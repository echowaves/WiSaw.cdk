## 1. Photos controllers

- [x] 1.1 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/photos/create.ts` before `psql.connect()`
- [x] 1.2 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/photos/delete.ts` before `psql.connect()`
- [x] 1.3 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/photos/watch.ts` before `psql.connect()`
- [x] 1.4 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/photos/unwatch.ts` before `psql.connect()`
- [x] 1.5 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/photos/getPhotoDetails.ts` before `psql.connect()`

## 2. Comments controllers

- [x] 2.1 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/comments/create.ts` before `psql.connect()`
- [x] 2.2 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/comments/delete.ts` before `psql.connect()`

## 3. Other controllers

- [x] 3.1 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/abuseReports/create.ts` before `psql.connect()`
- [x] 3.2 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/secrets/update.ts` before `psql.connect()`
- [x] 3.3 Add `assertValidUuid(uuid, 'uuid')` to `lambda-fns/controllers/messages/generateUploadUrlForMessage.ts` before `psql.connect()`
