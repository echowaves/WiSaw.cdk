## 1. GraphQL Resolver Dispatch Spec

- [x] 1.1 Create `openspec/specs/graphql-resolver-dispatch/spec.md` with requirements for the single-Lambda dispatcher pattern, handler registry interface, query/mutation separation, controller import conventions, and AppSyncEvent interface

## 2. Controller Conventions Spec

- [x] 2.1 Create `openspec/specs/controller-conventions/spec.md` with requirements for async function signatures, connect-query-clean lifecycle, parameterized SQL, plainToClass usage, timestamp formatting, UUID generation, inline validation, domain directory organization, side effect composition, and PostGIS function usage

## 3. Database Access Patterns Spec

- [x] 3.1 Create `openspec/specs/database-access-patterns/spec.md` with requirements for ManagedServerlessClient wrapper, health checking, connection lifetime rotation, automatic reconnection, parameterized queries, PostGIS conventions, PascalCase table naming, query result access patterns, and connection cleanup

## 4. CDK Infrastructure Spec

- [x] 4.1 Create `openspec/specs/cdk-infrastructure/spec.md` with requirements for resource module organization, naming conventions, environment-prefixed resources, single Lambda data source resolver wiring, NodejsFunction configuration, sharp layer usage, Lambda@Edge patterns, scheduled Lambda rules, production-only resources, log group conventions, AppSync auth configuration, and externalized config

## 5. Data Model Conventions Spec

- [x] 5.1 Create `openspec/specs/data-model-conventions/spec.md` with requirements for plain TypeScript model classes, class-transformer instantiation, toJSON derived properties, UUID/integer ID duality, string timestamp typing, model file location, S3 URL patterns, and export style conventions
