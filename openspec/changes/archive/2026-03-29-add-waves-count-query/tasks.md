## 1. GraphQL schema

- [x] 1.1 Add `getWavesCount(uuid: String!): Int!` query to `graphql/schema.graphql`

## 2. Controller

- [x] 2.1 Create `lambda-fns/controllers/waves/getWavesCount.ts` following the `getUngroupedPhotosCount` pattern

## 3. Dispatcher

- [x] 3.1 Add import and handler entry for `getWavesCount` in `lambda-fns/index.ts`
